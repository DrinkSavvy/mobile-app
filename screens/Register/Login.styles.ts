import { LayoutContainer } from '@utils/spirokit-config'

import { InputFocusBorder } from './types'

export const inputBorderStyle: InputFocusBorder = { borderColor: 'primary.500' }

export const inputContainerStyle: LayoutContainer = { width: '100%' }
